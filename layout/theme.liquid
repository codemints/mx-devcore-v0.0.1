<!doctype html>

<html>
  <head>
    <title>{{ page_title }}</title>
    {{ content_for_header }}
  </head>
  
  <body>
    {%- sections 'group-header' -%}

    <main>
      {{ content_for_layout }}
    </main>

    {%- sections 'group-footer' -%}

    {%- sections 'group-cart' -%}
  </body>
</html>