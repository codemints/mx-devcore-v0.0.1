<!doctype html>

<html>
  <head>
    <title>{{ page_title }}</title>
    {{ content_for_header }}

    {%- style -%}
      :root {
        {%- render 's.variables.color' -%}
        {%- render 's.variables.spacing' -%}
        {%- render 's.variables.header' -%}
      }
    {%- endstyle -%}

    {{ 'theme-core.min.css' | asset_url | stylesheet_tag }}
  </head>
  
  <body>
    {%- sections 'group-header' -%}

    <main>
      {{ content_for_layout }}
    </main>

    {%- sections 'group-footer' -%}

    {%- sections 'group-cart' -%}
  </body>
</html>