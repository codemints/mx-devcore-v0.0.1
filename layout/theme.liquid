<!doctype html>

<html>
  <head>
    <title>{{ page_title }}</title>
    {{ content_for_header }}

    {%- style -%}
      :root {
        {%- render 'style.variables.header' -%}
      }
    {%- endstyle -%}
  </head>
  
  <body>
    {%- sections 'group-header' -%}

    <main>
      {{ content_for_layout }}
    </main>

    {%- sections 'group-footer' -%}

    {%- sections 'group-cart' -%}
  </body>
</html>