<div class="splide" ref="{{ prefix }}-ui" data-custom-variant="{{ custom }}">
  <div class="splide__track" ref="{{ prefix }}-track">
    <ul class="splide__list" ref="{{ prefix }}-list">
      {{ slides }}
    </ul>
  </div>
</div>